# Build output
/scribe
/bin/
/dist/

# Editor / OS
.DS_Store
*.swp
*.swo
*~
.idea/
.vscode/

# Go
/vendor/
*.test
*.out
coverage.txt

# Claude Code session state + research + drops (not for public repo)
# — except .claude/skills/, which holds project-scoped skills that travel with the repo
.claude/*
!.claude/skills/
.claude/skills/*
!.claude/skills/getscribe-site-sync/

# Logs (scribe writes these under /tmp by default; keep local copies out)
*.log
/tmp/

# Release workspace
.goreleaser/

# Secrets — never commit
.env

# site/ — source-only, no JS toolchain in repo
/site/.wrangler/
/site/node_modules/
/site/package.json
/site/package-lock.json
